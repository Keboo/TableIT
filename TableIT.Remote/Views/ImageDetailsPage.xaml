<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TableIT.Remote.ViewModels"
             xmlns:img="clr-namespace:TableIT.Remote.Imaging"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             x:Class="TableIT.Remote.Views.ImageDetailsPage"
             x:DataType="vm:ImageDetailsPageViewModel"
             BackgroundColor="{DynamicResource Background}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!--<Grid GridLayout.Row="0" Background="Red" VerticalOptions="Start" HorizontalOptions="Start">
      --><!--<Button Text="Select"
              VerticalOptions="Start"
              Command="{Binding SelectCommand}"/>--><!--
    </Grid>-->

    <!--<Button Text="Foo" GridLayout.Row="0" />-->

    <img:PinchToZoomContainer>
      <skia:SKCanvasView x:Name="Canvas" PaintSurface="Canvas_PaintSurface" />
    </img:PinchToZoomContainer>
    <!--<ScrollView GridLayout.Row="0"
                Orientation="Both"
                Background="Red">
      
      <Image Source="{Binding Image}" Aspect="Fill" Margin="10" WidthRequest="1800" HeightRequest="1200"/>
    </ScrollView>-->
  </Grid>
</ContentPage>
